import 'package:flutter/material.dart';

class DisplayReceipesBasedOnIngredientUserPreference extends StatelessWidget {
  const DisplayReceipesBasedOnIngredientUserPreference({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
